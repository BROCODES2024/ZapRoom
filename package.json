{
  "name": "zaproom-monorepo",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "start": "echo 'This package should not be started directly.'"
  }
}
